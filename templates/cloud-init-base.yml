#cloud-config
package_update: true
packages:
- qemu-guest-agent
users:
- name: ${username}
  sudo: ALL=(ALL) NOPASSWD:ALL
  shell: /bin/bash
  lock_passwd: True
  ssh_authorized_keys:
  - ${ssh_public_key}
runcmd:
- systemctl enable --now qemu-guest-agent
